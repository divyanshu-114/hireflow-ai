# HireFlow AI — Frontend environment
# Copy to .env.local and adjust as needed. Vite only exposes VITE_* vars.

# Base URL of the FastAPI backend (runs on port 8000).
# The frontend dev server itself runs on port 3000 (see vite.config.js).
VITE_API_BASE_URL=http://localhost:8000
