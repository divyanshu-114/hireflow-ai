name: Frontend CI

on:
  pull_request:
    branches:
      - dev
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'

jobs:
  frontend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Install dependencies
        working-directory: frontend
        run: npm install

      - name: Run frontend tests
        working-directory: frontend
        # --runInBand is a Jest flag; Vitest's equivalent is --no-file-parallelism
        # (run test files sequentially). Keeping the serial intent for CI stability.
        run: npm test -- --no-file-parallelism

      - name: Build frontend
        working-directory: frontend
        run: npm run build